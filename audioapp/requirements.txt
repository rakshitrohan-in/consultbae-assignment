Flask==3.1.3
pydub==0.25.1
Werkzeug==3.1.3

# Not a pip package: ffmpeg (which includes ffprobe) must be installed and
# on PATH. Used directly via subprocess for duration/sample-rate/bitrate,
# and indirectly by pydub to decode webm/mp3/m4a/ogg for loudness/noise
# analysis.
